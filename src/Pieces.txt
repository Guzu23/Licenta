/*
		if (e.getKeyCode() == KeyEvent.VK_W) {
			camera.moveForward();
			if (e.getKeyCode() == KeyEvent.VK_A) {
				camera.moveLeft();
				if (e.getKeyCode() == KeyEvent.VK_SPACE) {
					camera.moveUp();
					updatePlayerPosition(camera.PLAYER_SPEED, -camera.PLAYER_SPEED, camera.PLAYER_SPEED);
				}
				else if (e.getKeyCode() == KeyEvent.VK_K) {
					camera.moveDown();
					updatePlayerPosition(camera.PLAYER_SPEED, camera.PLAYER_SPEED, camera.PLAYER_SPEED);
				}
				else updatePlayerPosition(camera.PLAYER_SPEED, 0, camera.PLAYER_SPEED);
			}
			else if (e.getKeyCode() == KeyEvent.VK_D) {
				camera.moveRight();
				if (e.getKeyCode() == KeyEvent.VK_SPACE) {
					camera.moveUp();
					updatePlayerPosition(-camera.PLAYER_SPEED, -camera.PLAYER_SPEED, camera.PLAYER_SPEED);
				}
				else if (e.getKeyCode() == KeyEvent.VK_K) {
					camera.moveDown();
					updatePlayerPosition(-camera.PLAYER_SPEED, camera.PLAYER_SPEED, camera.PLAYER_SPEED);
				}
				else updatePlayerPosition(-camera.PLAYER_SPEED, 0, camera.PLAYER_SPEED);
			}
			else updatePlayerPosition(0, 0, camera.PLAYER_SPEED);
		}
		else if (e.getKeyCode() == KeyEvent.VK_S) {
			camera.moveBackward();
			if (e.getKeyCode() == KeyEvent.VK_A) {
				camera.moveLeft();
				if (e.getKeyCode() == KeyEvent.VK_SPACE) {
					camera.moveUp();
					updatePlayerPosition(camera.PLAYER_SPEED, -camera.PLAYER_SPEED, -camera.PLAYER_SPEED);
				}
				else if (e.getKeyCode() == KeyEvent.VK_K) {
					camera.moveDown();
					updatePlayerPosition(camera.PLAYER_SPEED, camera.PLAYER_SPEED, -camera.PLAYER_SPEED);
				}
				else updatePlayerPosition(camera.PLAYER_SPEED, 0, -camera.PLAYER_SPEED);
			}
			else if (e.getKeyCode() == KeyEvent.VK_D) {
				camera.moveRight();
				if (e.getKeyCode() == KeyEvent.VK_SPACE) {
					camera.moveUp();
					updatePlayerPosition(-camera.PLAYER_SPEED, -camera.PLAYER_SPEED, -camera.PLAYER_SPEED);
				}
				else if (e.getKeyCode() == KeyEvent.VK_K) {
					camera.moveDown();
					updatePlayerPosition(-camera.PLAYER_SPEED, camera.PLAYER_SPEED, -camera.PLAYER_SPEED);
				}
				else updatePlayerPosition(-camera.PLAYER_SPEED, 0, -camera.PLAYER_SPEED);
			}
			else updatePlayerPosition(0, 0, -camera.PLAYER_SPEED);
		}
		else if (e.getKeyCode() == KeyEvent.VK_A) {
			camera.moveLeft();
			if (e.getKeyCode() == KeyEvent.VK_SPACE) {
				camera.moveUp();
				updatePlayerPosition(camera.PLAYER_SPEED, -camera.PLAYER_SPEED, 0);
			}
			else if (e.getKeyCode() == KeyEvent.VK_K) {
				camera.moveDown();
				updatePlayerPosition(camera.PLAYER_SPEED, camera.PLAYER_SPEED, 0);
			}
			else updatePlayerPosition(camera.PLAYER_SPEED, 0, 0);
		}
		else if (e.getKeyCode() == KeyEvent.VK_D) {
			camera.moveRight();
			if (e.getKeyCode() == KeyEvent.VK_SPACE) {
				camera.moveUp();
				updatePlayerPosition(-camera.PLAYER_SPEED, -camera.PLAYER_SPEED, 0);
			}
			else if (e.getKeyCode() == KeyEvent.VK_K) {
				camera.moveDown();
				updatePlayerPosition(-camera.PLAYER_SPEED, camera.PLAYER_SPEED, 0);
			}
			else updatePlayerPosition(-camera.PLAYER_SPEED, 0, 0);
		}
		else if (e.getKeyCode() == KeyEvent.VK_SPACE) {
			camera.moveUp();
			updatePlayerPosition(0, -camera.PLAYER_SPEED, 0);
		}
		else if (e.getKeyCode() == KeyEvent.VK_K) {
			camera.moveDown();
			updatePlayerPosition(0, camera.PLAYER_SPEED, 0);
		}
		
		if (e.getKeyCode() == KeyEvent.VK_ESCAPE) System.exit(0);
		
		
	    */